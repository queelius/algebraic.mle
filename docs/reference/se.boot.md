# Function for obtaining an estimate of the standard error of the bootstrap of the MLE `object`.

Function for obtaining an estimate of the standard error of the
bootstrap of the MLE `object`.

## Usage

``` r
# S3 method for boot
se(object)
```

## Arguments

- object:

  the bootstrapped MLE object
