# Function for obtaining the fisher information matrix of an \`mle\` object.

Function for obtaining the fisher information matrix of an \`mle\`
object.

## Usage

``` r
# S3 method for mle
fim(x, ...)
```

## Arguments

- x:

  the \`mle\` object to obtain the fisher information of.

- ...:

  pass additional arguments
