# log-likelihood function generator given data \`x\` for the exponential distribution

log-likelihood function generator given data \`x\` for the exponential
distribution

## Usage

``` r
exp_rate_loglike(x)
```

## Arguments

- x:

  data
