# Function for obtaining a summary of `object`, which is a fitted `boot` object.

Function for obtaining a summary of `object`, which is a fitted `boot`
object.

## Usage

``` r
# S3 method for boot
summary(object, ...)
```

## Arguments

- object:

  the `boot` object

- ...:

  pass additional arguments
