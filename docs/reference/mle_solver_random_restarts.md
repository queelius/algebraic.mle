# Iterative MLE method using gradient ascent

Iterative MLE method using gradient ascent

## Usage

``` r
mle_solver_random_restarts(mle_solver, start_gen, trials = 10)
```

## Arguments

- mle_solver:

  MLE solver, takes a starting point

- start_gen:

  starting point generator

- trials:

  number of trials to run
