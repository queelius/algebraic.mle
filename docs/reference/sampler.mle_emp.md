# Method for sampling from an \`mle_emp\` object.

Method for sampling from an \`mle_emp\` object.

## Usage

``` r
# S3 method for mle_emp
sampler(x, ...)
```

## Arguments

- x:

  the \`mle_emp\` object to create sampler for

- ...:

  additional arguments to pass
