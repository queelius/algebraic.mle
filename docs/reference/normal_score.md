# Score function generator given data \`x\` for the normal distribution

Score function generator given data \`x\` for the normal distribution

## Usage

``` r
normal_score(x)
```

## Arguments

- x:

  data (simple random sample)
