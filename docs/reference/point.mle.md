# Computes the point estimate of an \`mle\` object.

Computes the point estimate of an \`mle\` object.

## Usage

``` r
# S3 method for mle
point(x, ...)
```

## Arguments

- x:

  the \`mle\` object.

- ...:

  pass additional arguments
