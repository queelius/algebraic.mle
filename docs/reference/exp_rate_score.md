# score (derivative of log-likelihood) function generator given data \`x\` for the exponential distribution

score (derivative of log-likelihood) function generator given data \`x\`
for the exponential distribution

## Usage

``` r
exp_rate_score(x)
```

## Arguments

- x:

  data
