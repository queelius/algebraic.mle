# Method for determining the sample size of an `mle` object.

Method for determining the sample size of an `mle` object.

## Usage

``` r
# S3 method for mle
sample_size(x)
```

## Arguments

- x:

  the `mle` object
