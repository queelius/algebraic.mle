# score function generator given data \`x\` for the weibull distribution given a simple random sample.

score function generator given data \`x\` for the weibull distribution
given a simple random sample.

## Usage

``` r
weibull_shape_scale_score(x)
```

## Arguments

- x:

  data
