# score function generator given data \`x\` for the weibull distributison given a simple random sample.

score function generator given data \`x\` for the weibull distributison
given a simple random sample.

## Usage

``` r
weibull_score(x)
```

## Arguments

- x:

  data
