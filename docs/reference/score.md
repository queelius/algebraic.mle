# score

Generic function for computing the score of an estimator object with
respect to its log-likelihood.

## Usage

``` r
score(x, ...)
```

## Arguments

- x:

  the object to compute the score of.

- ...:

  pass additional arguments
