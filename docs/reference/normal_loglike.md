# log-likelihood function generator given data \`x\` for the normal distribution

log-likelihood function generator given data \`x\` for the normal
distribution

## Usage

``` r
normal_loglike(x)
```

## Arguments

- x:

  data
