# log-likelihood function generator given data `x` for the weibull distribution

log-likelihood function generator given data `x` for the weibull
distribution

## Usage

``` r
weibull_fisher_info(x)
```

## Arguments

- x:

  data
