# Computes the point estimate of an \`mle\` object.

Computes the point estimate of an \`mle\` object.

## Usage

``` r
# S3 method for mle_emp
point(x, ...)
```

## Arguments

- x:

  the \`mle\` object to compute the point estimate of

- ...:

  pass additional arguments
