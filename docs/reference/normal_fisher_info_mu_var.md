# Fisher information matrix generator given data \`x\` for the normal distribution

Fisher information matrix generator given data \`x\` for the normal
distribution

## Usage

``` r
normal_fisher_info_mu_var(x, observed = T)
```

## Arguments

- x:

  data (simple random sample)

- observed:

  whether to return the observed fisher information, default is TRUE
