% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_dist.R
\name{as_dist.mle}
\alias{as_dist.mle}
\title{Convert an MLE to a distribution object.}
\usage{
\method{as_dist}{mle}(x, ...)
}
\arguments{
\item{x}{An \code{mle} object.}

\item{...}{Additional arguments (not used).}
}
\value{
A \code{normal} (univariate) or \code{mvn} (multivariate) distribution.
}
\description{
Converts an \code{mle} object to its asymptotic
\code{\link[algebraic.dist]{normal}} or \code{\link[algebraic.dist]{mvn}}
distribution. The MLE must have a variance-covariance matrix available.
}
\examples{
fit <- mle(theta.hat = c(mu = 5), sigma = matrix(0.25))
d <- as_dist(fit)
mean(d)   # 5
vcov(d)   # 0.25

fit2 <- mle(theta.hat = c(a = 1, b = 2), sigma = diag(c(0.1, 0.2)))
d2 <- as_dist(fit2)
mean(d2)  # c(1, 2)
}
