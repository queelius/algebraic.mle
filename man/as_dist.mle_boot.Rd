% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_dist.R
\name{as_dist.mle_boot}
\alias{as_dist.mle_boot}
\title{Convert a bootstrap MLE to an empirical distribution.}
\usage{
\method{as_dist}{mle_boot}(x, ...)
}
\arguments{
\item{x}{An \code{mle_boot} object.}

\item{...}{Additional arguments (not used).}
}
\value{
An \code{empirical_dist} object.
}
\description{
Converts an \code{mle_boot} object to an
\code{\link[algebraic.dist]{empirical_dist}} built from the bootstrap
replicates.
}
\examples{
set.seed(123)
x <- rexp(50, rate = 2)
rate_mle <- function(data, indices) 1 / mean(data[indices])
boot_result <- boot::boot(data = x, statistic = rate_mle, R = 200)
fit <- mle_boot(boot_result)
d <- as_dist(fit)
mean(d)
}
