# Extract FIM from an MLE, falling back to ginv(vcov).

Extract FIM from an MLE, falling back to ginv(vcov).

## Usage

``` r
fim_or_ginv_vcov(m)
```

## Arguments

- m:

  An mle_fit object.

## Value

A matrix (FIM).
